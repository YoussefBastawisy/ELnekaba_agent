# Session 5 — HR Agent
# Install with:  pip install -r requirements.txt

streamlit>=1.40
langchain>=1.4
langgraph>=1.2
langchain-groq>=1.1
python-dotenv>=1.0
requests>=2.31
pandas>=2.0

# Semantic search. Large download (~2 GB, pulls PyTorch).
# If this fails or you are short on disk, comment BOTH lines out.
# The app falls back to keyword search automatically and still runs.
sentence-transformers>=3.0
scikit-learn>=1.4
